<% provide(:title, '') %>
<% if user_signed_in? %>
  <%= render 'microposts/micropost', microposts: @microposts %>
<% else %>
  <div class="jumbotron jumbotron-fluid">
    <div class="container-fluid jumbotron-container">
      <h1 class="display-4 ">#ootd</h1>
    </div>
  </div>
<% end %>
